name: Subtree Split

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag to split (e.g. v1.0.1). Leave empty to split the branch.'
        required: false
        type: string

# On a tag push: split that tag. On a manual dispatch with a tag: split it.
# On a plain branch push: empty, so the action splits the branch.
env:
  SPLIT_TAG: ${{ github.ref_type == 'tag' && github.ref_name || github.event.inputs.tag }}

jobs:
  split:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        package:
          - directory: packages/core
            repository: persona-core
          - directory: packages/livewire
            repository: persona-livewire
          - directory: packages/inertia
            repository: persona-inertia
          - directory: packages/blade
            repository: persona-blade
          - directory: packages/api
            repository: persona-api

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Split ${{ matrix.package.directory }}
        uses: symplify/monorepo-split-github-action@v2.3.0
        env:
          GITHUB_TOKEN: ${{ secrets.ACCESS_TOKEN }}
        with:
          package_directory: ${{ matrix.package.directory }}
          repository_organization: unknownman
          repository_name: ${{ matrix.package.repository }}
          tag: ${{ env.SPLIT_TAG }}
          user_name: unknownman
          user_email: unknownman@users.noreply.github.com
